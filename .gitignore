**/node_modules/
node_modules_old/
**/.pnpm-store/
**/.turbo/
**/.vite/
**/.next/
**/dist/
**/dist-pages/
**/storybook-static/
**/*.tsbuildinfo
*.log
node_modules_old/

# Environment / secrets
**/.env.local
**/.env.*.local
**/.env.development.local
**/.env.production.local

# Cookie jar from manual smoke tests
admin-cookie.txt
.vercel

# AgentHub orchestration scratch (session board, worktree coordination)
.agenthub/
# Harness-managed subagent worktrees
.claude/worktrees/

# Python bytecode (e.g. vendored skill scripts)
__pycache__/
*.pyc

# Environment files: ignore everything except the committed examples
.env
**/.env
**/.env.*
!**/.env.example
*.local

# Test coverage output
coverage/
**/coverage/

# Per-machine tool settings (Claude Code local permissions)
.claude/settings.local.json

# Local scratch / temporary AI-session output
_scratch/
**/*.tmp

# Personal material — never track (public repo; kept privately outside git)
resources/arsenale-mentale.html
apps/web-next/app/admin/(authed)/arsenale/

# Playwright artifacts
playwright-report/
test-results/
blob-report/
