{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["VITE_UI_SOURCE"],
  "tasks": {
    "dev": { "cache": false, "persistent": true },
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", "build/**", ".next/**"] },
    "lint": { "outputs": [] },
    "typecheck": { "outputs": [] },
    "test": { "outputs": [] }
  }
}
